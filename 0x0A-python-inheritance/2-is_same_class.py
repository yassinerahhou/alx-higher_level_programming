#!/usr/bin/python3
"""My fonction"""


def is_same_class(obj, a_class):
    """
    A function that returns True if the object is exactly a
    """
    if type(obj) is a_class:
        return True
    else:
        return False
