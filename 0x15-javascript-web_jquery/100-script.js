document.addEventListener("DOMContentLoaded", function () {
  // DOMContentLoaded : we use it when  we need to make sure that our js code dosen't run until the entire HTML document has been loaded
  document.querySelector("header").style.color = "#FF0000";
});
